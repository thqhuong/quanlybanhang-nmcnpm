<UserControl x:Class="quanlybanhang_nmcnpm.Views.OverviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tổng quan Hệ thống" FontSize="24" FontWeight="Bold" Foreground="#004C99" Margin="0,0,0,10"/>
        <Border Grid.Row="0" BorderBrush="#004C99" BorderThickness="0,0,0,1" Margin="0,35,0,20"/>

        <!-- Stats Cards -->
        <Grid Grid.Row="1" Margin="0,10,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Revenue -->
            <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="4" Padding="15" Margin="0,0,15,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="#E6F2FF" Width="50" Height="50" CornerRadius="4">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8B0;" FontSize="24" Foreground="#004C99" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="DOANH THU HÔM NAY" FontSize="11" Foreground="#666666" FontWeight="SemiBold"/>
                        <TextBlock Text="12,540,000 đ" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- New Orders -->
            <Border Grid.Column="1" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="4" Padding="15" Margin="0,0,15,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="#E8F5E9" Width="50" Height="50" CornerRadius="4">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE783;" FontSize="24" Foreground="#2E7D32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="ĐƠN HÀNG MỚI" FontSize="11" Foreground="#666666" FontWeight="SemiBold"/>
                        <TextBlock Text="42 đơn" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Low Stock -->
            <Border Grid.Column="2" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="4" Padding="15" Margin="0,0,15,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="#FFF8E1" Width="50" Height="50" CornerRadius="4">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE705;" FontSize="24" Foreground="#F57F17" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="SẮP HẾT HÀNG" FontSize="11" Foreground="#666666" FontWeight="SemiBold"/>
                        <TextBlock Text="5 sản phẩm" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- New Customers -->
            <Border Grid.Column="3" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="4" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="#F3E5F5" Width="50" Height="50" CornerRadius="4">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE716;" FontSize="24" Foreground="#6A1B9A" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="KHÁCH MỚI" FontSize="11" Foreground="#666666" FontWeight="SemiBold"/>
                        <TextBlock Text="12 khách" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Chart Placeholder -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" Background="White">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE9D9;" FontSize="48" Foreground="#CCCCCC" HorizontalAlignment="Center"/>
                <TextBlock Text="Khu vực hiển thị biểu đồ và báo cáo chi tiết" FontSize="16" Foreground="#999999" Margin="0,15,0,5" HorizontalAlignment="Center"/>
                <TextBlock Text="Chức năng này đang được phát triển." FontSize="14" Foreground="#BBBBBB" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>