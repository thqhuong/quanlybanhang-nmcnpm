<UserControl x:Class="quanlybanhang_nmcnpm.Views.OverviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tổng quan hệ thống" FontSize="24" FontWeight="Bold" Foreground="#004C99" Margin="0,0,0,10"/>
        <Border Grid.Row="0" BorderBrush="#004C99" BorderThickness="0,0,0,1" Margin="0,35,0,20"/>

        <Grid Grid.Row="1" Margin="0,10,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="4" Padding="15" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="DOANH THU HÔM NAY" FontSize="11" Foreground="#666666" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TodayRevenue, StringFormat={}{0:N0} đ}" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="4" Padding="15" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="ĐƠN HÀNG HÔM NAY" FontSize="11" Foreground="#666666" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TodayOrders, StringFormat={}{0} đơn}" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="4" Padding="15" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="SẮP HẾT HÀNG" FontSize="11" Foreground="#666666" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding LowStockProducts, StringFormat={}{0} sản phẩm}" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="4" Padding="15">
                <StackPanel>
                    <TextBlock Text="KHÁCH MỚI THÁNG NÀY" FontSize="11" Foreground="#666666" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding NewCustomersThisMonth, StringFormat={}{0} khách}" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" Background="White">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE9D9;" FontSize="48" Foreground="#CCCCCC" HorizontalAlignment="Center"/>
                <TextBlock Text="Dữ liệu tổng quan được tính từ cơ sở dữ liệu" FontSize="16" Foreground="#666666" Margin="0,15,0,5" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding StatusMessage}" FontSize="14" Foreground="#999999" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
