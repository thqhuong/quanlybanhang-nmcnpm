<UserControl x:Class="quanlybanhang_nmcnpm.Views.SalesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Background="#F1F3F5">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Padding="10">
                <Grid>
                    <TextBlock Text="BÁN HÀNG TẠI QUẦY" FontSize="16" FontWeight="Bold" Foreground="#004C99" VerticalAlignment="Center"/>
                    <Button HorizontalAlignment="Right" Content="&#xE749; In Lại HĐ" FontFamily="Segoe UI, Segoe MDL2 Assets" Background="#E0E0E0" BorderThickness="0" Padding="10,5" Height="30"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Product Entry & List -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Entry Area -->
                    <Border Grid.Row="0" Background="#F8F9FA" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Mã sản phẩm (F2)" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
                                <Border Background="White" BorderBrush="#0078D7" BorderThickness="2" Height="35">
                                    <Grid>
                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE721;" Foreground="#999999" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                        <TextBox Text="    NHẬP SP001..." Foreground="#999999" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="10,0,5,0" />
                                    </Grid>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                <TextBlock Text="Số lượng" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
                                <TextBox Text="1" Height="35" TextAlignment="Center" VerticalContentAlignment="Center" BorderBrush="#CCCCCC"/>
                            </StackPanel>

                            <Button Grid.Column="2" Background="#0078D7" Foreground="White" BorderThickness="0" Height="35" VerticalAlignment="Bottom">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe UI" Text="+" FontWeight="Bold" Margin="0,0,5,0"/>
                                    <TextBlock Text="Thêm"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- List Area -->
                    <ListView Grid.Row="1" BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Mã SP" Width="100"/>
                                <GridViewColumn Header="Tên hàng hóa" Width="200"/>
                                <GridViewColumn Header="SL" Width="60"/>
                                <GridViewColumn Header="Đơn giá" Width="120"/>
                                <GridViewColumn Header="Thành tiền" Width="120"/>
                            </GridView>
                        </ListView.View>
                        <ListViewItem>
                            <TextBlock Text="Không có dữ liệu" Foreground="#999999" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                        </ListViewItem>
                    </ListView>
                </Grid>
            </Border>

            <!-- Payment Info -->
            <Border Grid.Column="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="15">
                        <TextBlock Text="THÔNG TIN THANH TOÁN" FontWeight="Bold" Foreground="#333333" HorizontalAlignment="Center"/>
                    </Border>

                    <!-- Customer & Totals -->
                    <Border Grid.Row="1" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Khách hàng" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="Khách lẻ" Height="35" VerticalContentAlignment="Center" Padding="10,0" BorderBrush="#CCCCCC"/>
                                <Button Grid.Column="1" Content="&#xE721;" FontFamily="Segoe MDL2 Assets" Background="#E0E0E0" BorderThickness="0" Height="35"/>
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="Tổng tiền hàng:" Foreground="#666666"/>
                                <TextBlock Text="0 đ" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Giảm giá:" Foreground="#666666" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="0" TextAlignment="Right" Height="30" VerticalContentAlignment="Center" BorderBrush="#CCCCCC"/>
                            </Grid>

                            <Grid>
                                <TextBlock Text="VAT (8%):" Foreground="#666666"/>
                                <TextBlock Text="0 đ" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Payment Details -->
                    <Border Grid.Row="3" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="15">
                        <StackPanel>
                            <Grid Margin="0,0,0,15">
                                <TextBlock Text="KHÁCH CẦN TRẢ:" FontWeight="Bold" Foreground="#004C99"/>
                                <TextBlock Text="0 đ" FontSize="20" FontWeight="Bold" Foreground="#D32F2F" HorizontalAlignment="Right"/>
                            </Grid>

                            <TextBlock Text="Khách thanh toán" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBox Text="0" Height="40" FontSize="16" Foreground="#2E7D32" TextAlignment="Right" VerticalContentAlignment="Center" BorderBrush="#CCCCCC" Margin="0,0,0,15"/>

                            <Button Background="#2E7D32" Foreground="White" Height="45" BorderThickness="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8D7;" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="THANH TOÁN (F9)" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>