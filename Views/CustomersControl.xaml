<UserControl x:Class="quanlybanhang_nmcnpm.Views.CustomersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="QUẢN LÝ KHÁCH HÀNG &amp; TÍCH ĐIỂM" FontSize="18" FontWeight="Bold" Foreground="#004C99" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="&#xE710; Thêm Mới (F2)" FontFamily="Segoe UI, Segoe MDL2 Assets" Background="White" BorderBrush="#CCCCCC" Height="30" Padding="10,0" Margin="0,0,10,0" Foreground="#2E7D32"/>
                <Button Content="&#xE70F; Sửa (F3)" FontFamily="Segoe UI, Segoe MDL2 Assets" Background="White" BorderBrush="#CCCCCC" Height="30" Padding="10,0" Margin="0,0,10,0" Foreground="#004C99"/>
                <Button Content="&#xE74D; Xóa (F4)" FontFamily="Segoe UI, Segoe MDL2 Assets" Background="White" BorderBrush="#CCCCCC" Height="30" Padding="10,0" Foreground="#C62828"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Margin="0,40,0,0"/>

        <!-- Search -->
        <Border Grid.Row="1" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,15,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="2" Width="300" Height="30">
                    <Grid>
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE721;" Foreground="#999999" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        <TextBox Text="Số điện thoại, họ tên..." Foreground="#999999" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="30,0,5,0"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <ListView Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" AlternationCount="2" x:Name="CustomersListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="" Width="40">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Id}" Foreground="#999999" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Số Điện Thoại" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE77B;" Foreground="#999999" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Phone}" FontWeight="Bold" Foreground="#004C99" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Tên Khách Hàng" Width="250" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Điểm Tích Lũy" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding Points}" FontWeight="Bold" Foreground="#D32F2F" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE734;" Foreground="#D32F2F" VerticalAlignment="Center" FontSize="10"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Lần Mua Cuối" Width="180" DisplayMemberBinding="{Binding LastPurchase}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#E0E0E0" BorderBrush="#CCCCCC" BorderThickness="1,0,1,1" Padding="10,5">
            <TextBlock Text="Tổng số khách hàng: 4" Foreground="#333333"/>
        </Border>
    </Grid>
</UserControl>